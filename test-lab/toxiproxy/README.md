Create a proxy through `localhost:8474`, then apply one fixed toxic at a time:
latency, socket timeout, or reset-peer. The host oracle verifies expected retry
and duplicate POST behavior without Docker.
