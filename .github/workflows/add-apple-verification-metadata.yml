name: Add Apple verification metadata

on:
  push:
    branches:
      - agent/clean-verification-metadata

permissions:
  contents: write

jobs:
  generate:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: true
      - uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - name: Generate iOS dependency verification metadata
        run: |
          chmod +x gradlew
          ./gradlew --write-verification-metadata sha256 :composeApp:linkDebugFrameworkIosSimulatorArm64 --no-daemon
      - name: Commit generated verification metadata
        run: |
          if git diff --quiet -- gradle/verification-metadata.xml; then exit 1; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add gradle/verification-metadata.xml
          git commit -m "build: verify Apple dependency metadata"
          git push
